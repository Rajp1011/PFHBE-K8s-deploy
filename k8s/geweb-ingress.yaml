apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ge-web
  namespace: ge-dev
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ge-web
            port:
              number: 80